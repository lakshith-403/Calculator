#include <bits/stdc++.h>
#pragma once
using namespace std;
class solver{
    public:
        static void solveBrackets(std::string line);
        static std::string simpleSolve(std::string line , char O);
        static set<char> Set;
};